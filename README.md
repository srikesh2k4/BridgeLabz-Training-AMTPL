# C# Practice — Nov 1, 2025

## 📁 Folder
`BridgeLabzApp.BasicCSharp`

## 📄 File
`BasicCSharp.cs`

## 🧠 Topics Covered
- Variables & Data Types  
- Type Conversion (`Convert` class)  
- Operators (++, +=, %, etc.)  
- `var` and `const` usage  
- Constructor and Method Calls  
- Namespace & Class Structure  

## ⚙️ Methods
- `Variable()` → Demonstrates data types and limits  
- `TypeConverstion()` → Converts strings to numeric types  
- `Operator()` → Shows arithmetic and assignment operators  
- `BasicCSharp()` → Constructor calls all above methods  

## 🚀 Run
```bash
dotnet run
